# LaundryConnect Mobile (Flutter)

Not started yet — arrives in Milestone 5 (see [docs/ROADMAP.md](../../docs/ROADMAP.md)).

Planned: Flutter/Dart, Android-first, navy/teal minimalist technician-focused
UI centred on universal search. Provider credentials are never stored in this
app — all provider access goes through the backend API.
