# LaundryConnect API configuration
# Copy to .env for local development. NEVER commit a real .env file.
# All values here are placeholders — see docs/SECURITY.md.

# development | test | production
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO

# PostgreSQL connection (used from Milestone 4 onwards; optional until then).
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:5432/DATABASE
DATABASE_URL=

# Comma-separated allowed CORS origins (e.g. the admin portal dev server).
CORS_ORIGINS=

# Provider connectors to enable. "mock" is sample data; "alliance" defaults
# to fixture mode (recorded/synthetic data, no live requests).
ENABLED_PROVIDERS=mock

# Per-provider search timeout in seconds.
PROVIDER_TIMEOUT_SECONDS=10

# --- Alliance connector -------------------------------------------------------
# Mode: fixture (default, no network, CI-safe) | session | credential.
ALLIANCE_MODE=fixture
# Path to the operator's authenticated browser session — set for session mode.
# MUST be OUTSIDE the repository; never committed; never logged. Create it with
# `python -m app.providers.alliance.bootstrap` (manual login).
# ALLIANCE_SESSION_PATH=/Users/you/.laundryconnect/alliance-session.json
# Master live gate — mirrors docs/PROVIDER_ACCESS/alliance-laundry-systems.md
# (CONDITIONALLY APPROVED). Live modes refuse until this is true; keep FALSE
# until the pre-first-request review is approved. Enabling it is a deliberate
# per-environment opt-in.
ALLIANCE_ACCESS_APPROVED=false
# Kill switch: set true to immediately disable Alliance live mode (e.g. if
# Alliance objects or publishes contrary terms).
ALLIANCE_LIVE_KILL_SWITCH=false
# Live-fetch controls (conservative). Login is on portal.alliancels.net;
# parts/model search is on the Parts Connection host pc.alliancels.net.
ALLIANCE_BASE_URL=https://portal.alliancels.net
ALLIANCE_ALLOWED_HOSTS=portal.alliancels.net,pc.alliancels.net
# Full search URL template ({query} is URL-encoded and substituted).
ALLIANCE_SEARCH_URL=https://pc.alliancels.net/en/Search/StartsWith?searchString={query}&x.Show=Assembly
ALLIANCE_RATE_LIMIT_PER_MINUTE=12
ALLIANCE_REQUEST_TIMEOUT_SECONDS=20
ALLIANCE_MAX_RETRIES=2
# Single-flight by default (no internal fan-out).
ALLIANCE_MAX_CONCURRENCY=1
# Longest a 429 Retry-After is honoured before giving up.
ALLIANCE_MAX_RETRY_AFTER_SECONDS=60
# Download caps: bound memory / reject unbounded transfers.
ALLIANCE_MAX_RESPONSE_BYTES=5242880
ALLIANCE_MAX_DOCUMENT_BYTES=104857600
ALLIANCE_DOWNLOAD_TIMEOUT_SECONDS=60

# Provider credentials are NEVER read from here or from files. If/when the
# provider terms permit automated login, credentials come only from the
# deployment environment or a secret manager — never committed, never logged.
