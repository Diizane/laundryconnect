"""Provider connector framework.

Every external information source (Alliance, Girbau, Richard Jay, mock) is
integrated behind the `ProviderConnector` interface and returns normalised
internal models. See docs/PROVIDER_CONNECTORS.md.
"""
